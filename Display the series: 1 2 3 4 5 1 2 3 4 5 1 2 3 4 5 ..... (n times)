#include <stdio.h>

int main() {
    int n;

    printf("Enter the number of times to repeat the series: ");
    scanf("%d", &n);

    printf("Series: ");
    for (int i = 1; i <= n; i++) {
        printf("%d ", (i % 5) + 1);
    }

    return 0;
}
